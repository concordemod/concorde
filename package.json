{
  "name": "concorde",
  "version": "1.0.0",
  "description": "A client mod for Discord.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "buildAsar": "node scripts/builder/buildAsar.mjs",
    "buildBase": "node scripts/builder/buildBase.mjs",
    "build": "pnpm run buildAsar && pnpm run buildBase",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "neeeerrrdd",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@electron/asar": "^3.2.10",
    "@types/node": "^20.13.0",
    "esbuild": "^0.21.4"
  }
}
